⚠ THIS PATH IS NOT SERVED BY VERCEL — root-level non-HTML files 404 on this
deployment. Confirmed 2026-08-17 (W3.9h): this file, /README.md, /robots.txt
and /vercel.json all return the same 79-byte 404, and always have. The
working deploy check is now /version.html — fetch that instead:

  curl -s https://steamteams-cc-portal.vercel.app/version.html

Left in place, unmodified below, as the historical record of the first two
silent-deploy failures this project hit. Do not use this path for anything
going forward — it can never return a match.

--------------------------------------------------------------------------

STEAM Teams CC Portal — deploy marker
wave:   W3.9d-f
commit: contain cross-tab session destruction; staffRole from staff_accounts (10.4.3 labels)
date:   2026-08-17

Why this file exists
--------------------
Twice on 2026-08-16/17 the code was correct and the record said shipped, but the
site was serving something older: once because the push went to the docs repo
instead of this one, and once because Vercel's GitHub webhook did not fire and
production stayed on the previous build. Both failed quietly.

This file is the cheap check. After any push, fetch /version.txt and confirm the
wave line matches what you just pushed. If it does not, the deploy did not land,
whatever the commit history says.

  curl -s https://steamteams-cc-portal.vercel.app/version.txt

Update the wave and commit lines with each deploy. Working Standards section 2:
"delivered means committed, pushed, and seen live."
